<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body>
  <% include ../partials/head %>

  <% for (let url in data) { %>
  <% if (shortUrl == url) { %>
  <p>
    <%= data[url]  %>
  </p>
  <% } %>
  <% } %>
</body>

</html>