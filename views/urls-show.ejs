<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body>
  <% include ./partials/_header %>

  <% for (let url in data) { %>
  <% if (shortUrl == url) { %>
  <p>
    <%= data[url]  %>
  </p>
  <% } %>
  <% } %>
</body>

</html>